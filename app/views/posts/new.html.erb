<div class="main posts-new">
  <div class="container">
    <h1 class="form-heading">Tweet a post</h1>
    <%= form_tag("/posts/create") do %>
      <div class="form">
        <div class="form-body">
          <% @post.errors.full_messages.each do |message| %>
            <div>
              <%= message %>
            </div>
          <% end %>
          <textarea name="content"><%= @post.content %></textarea>
          <input type="submit" value="tweet">
        </div>
      </div>
    <% end %>
  </div>
</div>



